# AGENTS.md — SBE（Seek-Battle-Evacuate）

单人撤离类 Roguelike 原型。Unity 2022.3.62f2 + URP 2D，框架为 GameFramework（UGF，源码在 `Assets/GameFramework/`），配置表用 Luban，插件有 UniTask、DOTween。

## 验证纪律（最重要）

- **不要启动 Unity、不要进入 Play Mode、不要执行 `dotnet build` / `msbuild` 等任何编译命令**（`Docs/Tech/04` 里程碑记录里出现的 `dotnet build SepCore.Runtime.csproj` 是历史做法，现不再由 agent 执行）。
- Agent 只做**静态检查**：通读代码、grep 交叉引用、推演逻辑边界，并在交付说明中列出潜在编译/逻辑问题。
- 编译是否通过、EditMode 测试、Play Mode 验收等一切需要占用 Unity Editor 进程的验证，**全部留给用户执行并回填结果**；不要在文档中替用户勾选完成状态。

## 主要目录

| 路径 | 内容 |
| --- | --- |
| `Assets/GameMain/Scripts/Base/` | 程序集 `SepCore.Base`：常量、存档数据结构（`Definition/DataStruct/`）、Luban 生成代码（`Gen/`） |
| `Assets/GameMain/Scripts/Runtime/` | 程序集 `SepCore.Runtime`：战斗内核（`Battle/`）、自定义组件（`CustomComponent/` 下 Luban、Random、Run、Save）、UIBase |
| `Assets/GameMain/Scripts/UI/` | 程序集 `SepCore.Presentation`：各界面 Form/View |
| `Assets/GameMain/Scripts/Procedure/`、`Editor/` | UGF 流程、编辑器工具 |
| `Assets/GameMain/Scripts/Tests/EditMode/` | EditMode 测试（当前全部针对 `BattleRuntime` 纯逻辑） |
| `数据表/` | Luban 源：`Datas/*.xlsx`（`__tables__.xlsx` 注册）、`Defines/`、导出脚本 `gen_cli.sh` / `gen_cli.bat` |
| `Docs/GameDesign/`、`Docs/Tech/` | 设计文档；技术文档（Luban、存档、UI、战斗 TODO） |

## 生成代码不可手改

- `Scripts/Base/Gen/` 全部由 Luban 导出。改表只改 `数据表/Datas/` 源表，再运行 `数据表/gen_cli.sh`（Windows 也可 `gen_cli.bat`）。
- `Scripts/UI/` 下的 `XxxForm.cs` / `XxxView.cs` 由 `UIAssetsTools` 生成（文件头有 `// <auto-generated />`）。业务逻辑写在同名 `XxxForm.Logic.cs` partial 中；prefab 上要暴露给代码的组件在 `UISerializationItem` 登记变量名后重新构建（编辑器菜单 `Utility/UI/Build UI Prefabs`）。

## 架构边界

- 战斗内核 `BattleRuntime` 是可独立测试的纯逻辑：**不读写 `SaveData`、不访问场景对象/地图敌人/掉落/局外结算**；外部统一通过 `TurnBattleComponent` 启动战斗、提交指令、接收结果。
- 随机数走同一个本局随机源（`IRunRandomSource` / `RandomComponent`），战斗不重新播种。

## UI 常见坑（详见 `Docs/Tech/03_UIMaintenance.md`）

- UGF 只对顶级 Form 调用 `OnInit/OnOpen/OnClose` 生命周期；**嵌套 Form 的这些方法永远不会执行**。事件绑定用幂等模式（`_listenersBound` 标志 + `EnsureXxx()`），放在能力入口（如 `Refresh`）里。
- 子 Form 只提供能力（public 方法，幂等可重入），数据由调用方传入；只有顶级 Form 有编排权。子组件向上通信用 `GameEntry.Event.Fire` + `XxxEventArgs`（放 `Scripts/Base/Event/UIForm/`，`ReferencePool` + `Create`/`Clear`），禁止反向持有父 Form。

## Luban 约定（详见 `Docs/Tech/01_LubanDataTable.md`）

- 表名小写 `tbxxxconfig`：Excel 表名、导出文件名、`Tables` 构造参数、`TableNames` 数组四处必须一致。
- 访问入口 `GameEntry.Luban.Get<T>(id)` / `GetTable<T>()`；单行表（`mode=one`，如 `tbglobalconfig`）不注册访问器，走 `Tables.TbXxxConfig.Data`。

## 判空与错误处理

- 声明处已经初始化赋值的变量，之后**不再对它判空**。
- 不做静态回退（不用默认值/兜底逻辑静默继续）；异常路径直接 `Log.Error(...)` 抛出（UGF 的 `Log`，支持格式串，如 `Log.Error("Entity data is invalid.")`）。
- UI 的序列化字段（`[SerializeField]` 及 View 生成的引用）**不做判空检查**，默认已在 Inspector 中赋值。

## 代码风格

- 命名：public 成员大驼峰（`EncounterId`、`RoundNumber`）；私有字段 `_` 前缀 + 小驼峰（`_random`、`_view`），序列化字段写作 `[SerializeField] private XxxView _view = null;`；方法（含 private）大驼峰。
- 程序集拆分为 `SepCore.Base / Runtime / Presentation / Procedure / Editor` + `Tests`（仅 Editor 平台）+ `Luban.Runtime`；跨程序集引用参考各 `.asmdef` 的 GUID 列表。
- 命名空间跟随程序集根（如 `SepCore.Battle`、`SepCore.Definition`）。

## 开始改动前先读

- `Docs/Tech/TODO.md` — 研发路线与当前里程碑（阶段 0 战斗已完成，当前主线为阶段 1 局外经济、顶级 UI 架构与消耗品闭环）、里程碑完成纪律、架构与数据所有权。**每次只做当前里程碑，不预留接口提前实现。**
- 对应领域的 `Docs/Tech/01~03` 与 `Docs/GameDesign/` 系列设计文档。
